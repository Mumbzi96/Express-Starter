<style>  
    .sidebar.open ~ .home-section{
        left: 260px;
        width: calc(100% - 260px);
    }
    .initial_decision_form{
        grid-column: 3/4;
        display: flex;
        justify-content: space-evenly;
        align-items: center;
        width: 60%;
        margin-left: 20%;
        margin-right: 20%;
    }

    .initial_decision_button{
        background-color: #2D9CDB;
        border: none;
        color: #ffffff;
        padding: 12px 50px;
        border-radius: 8px;
        cursor: pointer;
        transition: background-color 0.3s ease-in-out;
        font-weight: bold;
        font-size: 16px;
        grid-column: 1/3;
        transform: scale(1.03); /* scales the div 3% larger on hover */
        transition: transform 0.3s ease-in-out; /* smooth transition effect for scaling */
    }

    .initial_decision_button:hover{
        background-color: #2C8ABD;
    }


    .home-section-container{
        padding-top: 70px;
    }

    .container {
        display: grid;
        grid-template-columns:repeat(6, 1fr);
        grid-template-rows: repeat(4, 150px);
        gap: 20px;
        align-content: center;
        padding: 20px;
    }

    /* =========== Header ========= */
    .header{
        margin: 0 auto;
        width: 94%;
        padding: .8rem 5px;
        color: #122543;
        display: flex;
        flex-direction: column;
    }

    .headerContent{
        {{#if (isEqual navdata.language "English")}}
        margin-left: 10px;
        {{else}}
        margin-right: 10px;
        {{/if}}
        margin-bottom: 5px;
        border-bottom: 3px solid black;
    }

    .header .back-icon{
        color: #122543;
        border-radius: 50%;
        display: flex;
        text-decoration: none;
        padding-top: 5px;
    }
    .header .back-icon i{
        font-size: 22px;
    }
    .header .back-icon:hover{
        color: #e2ff65;
    }

    .header .title{
        display: flex;
        align-items: center;
    }

    /* =========== table ===========*/
    main.table {
        position: relative;
        width: 82vw;
        max-width: 950px;
        background-color: #fff;

        backdrop-filter: blur(7px);
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        border-radius: .8rem;

        margin: 0 auto;
        overflow: hidden;
        grid-column: 2/6;
        grid-row: 1/3;
    }

    table {
        width: 100%;
    }

    /* ---------- Table Header ---------- */
    .table__header {
        width: 100%;
        background-color: #122543;
        padding: .8rem 1rem;
        color: white;
        display: flex;
        justify-content: space-between;
    }

    /* ---------- Search bar ---------- */
    .table__header .input-group {
        width: 35%;
        height: 100%;
        background-color: #fffa;
        padding: 0 .8rem;
        border-radius: 2rem;
        color: #0b1629;
        display: flex;
        justify-content: center;
        align-items: center;
        transition: .2s;
    }

    input[type="search"]::-webkit-search-clear-button,
    input[type="search"]::-webkit-search-decoration,
    input[type="search"]::-webkit-search-results-button,
    input[type="search"]::-webkit-search-results-decoration {
        display: none; 
    }

    /* ---------- Error Message ---------- */
    .table__header .error span{
        border-radius: 10px;
        padding: 5px;
    }

    .table__header .input-group:hover {
        width: 45%;
        background-color: #fffc;
        box-shadow: 0 .1rem .4rem #0002;
    }

    .table__header .input-group input {
        width: 100%;
        padding: 0 .5rem 0 .3rem;
        background-color: transparent;
        border: none;
        outline: none;
    }

    /* ---------- Table Body ---------- */
    .table__body {
        width: 95%;
        max-height: 200px;
        background-color: #fff;

        margin: .8rem auto;
        border-radius: .6rem;

        overflow: auto;
        overflow: overlay;
    }

    .table__body::-webkit-scrollbar{
        width: 0.3rem;
        height: 0.3rem;
    }

    .table__body::-webkit-scrollbar-thumb{
        border-radius: .5rem;
        background-color: #0004;
        visibility: hidden;
    }

    .table__body:hover::-webkit-scrollbar-thumb{ 
        visibility: visible;
    }

    table, th, td {
        border-collapse: collapse;
        padding: 0 10px;
        {{#if (isEqual navdata.language "English")}}
        text-align: left;
        {{else}}
        text-align: right;
        {{/if}}
    }

    thead th {
        position: sticky;
        top: 0;
        left: 0;
        cursor: pointer;
        text-transform: capitalize;
        background-color: white;
    }

    tbody tr:nth-child(even) {
        background-color: #0000000b;
    }

    tbody tr {
        --delay: .1s;
        transition: .5s ease-in-out var(--delay), background-color 0s;
    }

    tbody tr.hide {
        opacity: 0;
        transform: translateX(100%);
    }

    tbody tr:hover {
        background-color: #e2ff65 !important;
    }

    tbody tr td,
    tbody tr td p,
    tbody tr td img {
        transition: .2s ease-in-out;
    }

    tbody tr.hide td,
    tbody tr.hide td p {
        padding: 0;
        font: 0 / 0 sans-serif;
        transition: .2s ease-in-out .5s;
    }

    tbody tr.hide td img {
        width: 0;
        height: 0;
        transition: .2s ease-in-out .5s;
    }

    /* ========== Research Status ========== */
    .approved {
        color: #006b21;
    }
    .declined {
        color: #b30021;
    }
    .pending {
        color: #ebc474;
    }


    thead th span.icon-arrow {
        display: inline-block;
        width: 1.3rem;
        height: 1.3rem;
        border-radius: 50%;
        border: 1.4px solid transparent;
        
        text-align: center;
        font-size: 1rem;
        
        margin-left: .5rem;
        transition: .2s ease-in-out;
    }
    .downloadFile{
        text-decoration: none;
        color: black;
    }


    /* ========== The 3 parts outer (researches container), description, comment ========== */
    .outer, .description, .comment {
        border-radius: 10px;
        padding: 20px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        transition: transform 0.3s ease-in-out; /* smooth transition effect for scaling */
    }

    .outer:hover, .description:hover, .comment:hover {
        transform: scale(1.03); /* scales the div 3% larger on hover */
        transition: transform 0.3s ease-in-out; /* smooth transition effect for scaling */
        cursor: pointer;
    }

    /* ---------- Outer part (Researches Container) ---------- */
    .outer {
        grid-row: 2 / 4;
        background-color: #8cc4df;
        color: #0b1629;
        border-radius: 10px;
        padding: 20px;
        border: 2px solid #122543;
    }

    .outer-header{
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .outer .research{
        margin: 0 auto;
        margin-top: 10px;
        background-size: cover;
        height: 420px;
        width: 350px;
        border-radius: 10px;
    }

    /* ---------- The Research Card ---------- */
    .card-container {
        display: flex;
        flex-direction: column;
        align-items: center; /* Center cards horizontally */
        margin: -10 auto;
    }

    .card-container .card {
        width: 270px;  /* Or whatever width you prefer */
        height: 340px;  /* Or whatever height you prefer */
        background-color: #e0e0e0;  /* A light grey background color */
        padding: 10px 5px;  /* Padding inside each card */
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);  /* A simple shadow to give depth */
        border-radius: 10px;  /* Rounded corners */
        text-align: center;  /* Center the card content */
        font-size: 20px;  /* Set font size */
        transition: transform 0.3s; /* Smooth transition */
    }

    /* ---------- First Research Card ---------- */
    .card-container .card0{
        z-index: 1;
        background-color: #122543;
    }
    /* ---------- Second Research Card ---------- */
    .card-container .card1{
        margin: -300px 0; 
        background-color: #8cc4df;
        z-index: 2;
    }
    /* ---------- Third Research Card ---------- */
    .card-container .card2{
        margin:  0;  /* 10px vertical margin to separate the cards */
        background-color: #0b1629;
        z-index: 3;
    }

    .card:hover{
        transform: scale(1.02);
    }
    /* ---------- The Research Image ---------- */
    .card-container img{
        border-radius: 10px;
        width: 250px;
        height: 320px;
    }

    /* ---------- Description Part ---------- */
    .description {
        background-color: #e2ff65;
        color: black;
        border-radius: 10px;
        padding: 20px;
        max-width: 475px;
        grid-column: 2/4;
        grid-row: 3/5;
    }

    /* ---------- Comment Part ---------- */
    .comment, .description {
        overflow-x: hidden;  /* Ensures text doesn't overflow horizontally */
        overflow-y: auto;  /* Only shows scrollbar when necessary */
    }

    .comment {
        background-color: #0b1629;
        color: #8cc4df;
        border-radius: 10px;
        padding: 20px;
        max-width: 475px;
        grid-column: 4/6;
        grid-row: 3/5;
    }

    .comment-container {
        background-color: #122543;  /* Slightly darker shade than the main comment div for contrast */
        border-radius: 8px;
        margin: 10px 0;  /* Margin at the top and bottom for spacing between comments */
        padding: 10px 15px;  /* Padding on all sides for spacing */
        box-shadow: 0 3px 6px rgba(0, 0, 0, 0.15);  /* Gentle shadow for depth */
        overflow: hidden;  /* This hides any overflowing content */
        word-wrap: break-word;  /* This ensures that words that are too long will wrap onto the next line */
    }

    .comment-author {
        font-weight: bold;
        color: #e2ff65;  /* A contrasting color from your palette for the author's name */
        margin-bottom: 5px;  /* Space between author and comment content */
        font-size: 18px;  /* Making the author's name slightly bigger */
        border-bottom: 2px solid #8cc4df;
        margin-bottom: 10px;

    }

    .comment-content {
        background-color: rgba(140, 196, 223, 0.2);  /* A subtle background using a color from your palette */
        padding: 8px;
        border-radius: 6px;  /* Rounded edges for the content background */
        color: #8cc4df;  /* Using the palette color for the comment text */
    }

    /* For Chrome, Safari, and Edge */
    .comment::-webkit-scrollbar, .description::-webkit-scrollbar {
        display: none;
    }

    .comment::-webkit-scrollbar-track, .description::-webkit-scrollbar-track {
        background-color: #122543;
        border-radius: 10px;
    }

    .comment::-webkit-scrollbar-thumb, .description::-webkit-scrollbar-thumb {
        background-color: #8cc4df;
        border-radius: 10px;
    }

    /* ========== Expand Divs (Description and Comments Parts) ========== */
    .expandable {
        position: relative; 
        /* Smooth transitions for the properties */
        transition: transform 0.8s ease-in-out, top 0.8s ease-in-out, left 0.8s ease-in-out, width 0.8s ease-in-out, height 0.8s ease-in-out;

    }

    /* Hover effect for the expandable divs */
    .expandable:hover {
        transform: scale(1.05); /* Example hover effect */
        transition: transform 0.5s ease-in-out; /* Smooth scaling */
    }

    /* Stop hover effects when the div is expanded */
    .expandable.expanded,
    .expandable.expanded:hover {
        transform: none; /* Resetting any transformations */
        transition: none; /* Disabling transitions */
        cursor: auto;
    }

    /* ---------- close icon for (Description and Comment) ---------- */
    .closeDiv {
        position: absolute;
        top: 10px;
        {{#if (isEqual navdata.language "English")}}
        right: 10px;
        {{else}}
        left: 10px;
        {{/if}}
        color: #aaa;
        font-size: 28px;
        font-weight: bold;
        cursor: pointer;
        display: none; /* Initially hidden */
    }

    .closeDiv:hover,
    .closeDiv:focus {
        color: #8cc4df;
        text-decoration: none;
        cursor: pointer;
    }

    

    .expanded {
        z-index: 20; /* Make sure it's above other content */
        transform: scale(1.5); /* Enlarge the div */
        grid-column: 3/5;
        grid-row: 1/4;
        width: 100%;
        height: 100%;
        overflow-y: auto; /* In case the content is too long, show a scrollbar */
        transition: all 0.5s ease;
    }

    .sidebar.open ~ .expanded{
        width: calc((92% - 260px));
    }

    .expanded .closeDiv {
        display: block; /* Show the close button */
    }


    /* ========== Decision Button (for committee member in pending researches) ========== */
    .button-con{
        display: flex;
        justify-content: space-between;
        align-items: center;
    }
    .button-container{
        width: 100%;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }
    .decisionButton {
        background-color: #2D9CDB;
        border: none;
        color: #ffffff;
        padding: 12px 20px;
        border-radius: 8px;
        cursor: pointer;
        transition: background-color 0.3s ease-in-out;
        font-weight: bold;
        font-size: 16px;
        transform: scale(1.03); /* scales the div 3% larger on hover */
        transition: transform 0.3s ease-in-out; /* smooth transition effect for scaling */
    }

    #decisionButton{
        grid-column: 1/2;
    }

    #finalDecisionButton{
        grid-column: 2/3;
    }

    .decisionButton:hover {
        background-color: #2C8ABD;
    }

    .decisionModal {
        display: none;
        position: fixed;
        top: 0;
        width: 100vw;
        height: 100vh;
        background-color: rgba(0, 0, 0, 0.5);
        justify-content: center;
        align-items: center;
        z-index: 99;
    }

    .decisionModal h2{
        display: flex;
        justify-content: center;
    }

    .decisionModal-content p {
        font-style: italic;
        color: #555;
        margin-top: 10px;
    }

    .decisionModal .decisionModal-content {
        background-color: #ffffff;
        width: 400px;
        max-height: 80vh;
        overflow-y: auto;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
        position: relative;
        padding: 25px;
        top: 25%;
        {{#if (isEqual navdata.language "English")}}
        left: 35%;
        {{else}}
        right: 35%;
        {{/if}}
    }

    .decisionModal .decisionModal-content .decisionClose-modal {
        position: absolute;
        top: 10px;
        right: 10px;
        font-size: 24px;
        cursor: pointer;
        color: #555;
    }

    .decisionModal .decisionModal-content .decisionClose-modal:hover {
        color: #888;
    }

    /* ---------- Form inside the Decision Modal ---------- */
    /* ---------- The Form Part ---------- */
    .decisionModal .decisionModal-content #approvalForm {
        width: 100%;
        margin-top: 20px;
    }

    /* ---------- div contains the radio button ---------- */
    .radio-group {
        display: flex;
        justify-content: space-between;
        margin-bottom: 20px;
    }

    /* ---------- the labels of the radio button ---------- */
    .custom-radio {
        background-color: #E9E9E9;
        color: #555;
        padding: 8px 16px;
        border-radius: 8px;
        cursor: pointer;
        transition: background-color 0.3s ease-in-out;
    }

    /* ---------- The radio button (approve & decline) ---------- */
    .approvalForm input[type="radio"]:checked + .custom-radio {
        background-color: #2D9CDB;
        color: #ffffff;
    }

    /* ---------- div contains the decline reason ---------- */
    #declineReasonContainer {
        display: none;
        margin-top: 20px;
    }

    /* ---------- textarea of the decline reason ---------- */
    #declineReason {
        width: 100%;
        height: 150px;
        padding: 10px;
        border: 1px solid #E0E0E0;
        border-radius: 8px;
        color: #555;
        background-color: #F7F7F7;
        resize: none;
    }

    /* ---------- The submit button ---------- */
    .approvalForm button {
        width: 100%;
        margin-top: 25px;
        margin-left: 5px;
        padding: 12px 20px;
        background-color: #2D9CDB;
        border: none;
        border-radius: 8px;
        color: #ffffff;
        cursor: pointer;
        transition: background-color 0.3s ease-in-out;
        font-weight: bold;
    }

    .approvalForm .button1{
        margin-right: 2.5px;
    }

    .approvalForm .button2{
        margin-left: 2.5px;
    }

    .approvalForm button:hover {
        background-color: #2C8ABD;
    }

    /* ======== Download all research button ========== */
    /* ---------- anchor to download pdf ---------- */
    .hidden-pdf-link {
        display: none;
    }

    /* ---------- download all pdf at sAME TIME ---------- */
    #downloadAll {
        background-color: #3498db; /* Blue color */
        color: white;              /* Text color */
        border: none;              /* Removes any border */
        border-radius: 50%;        /* Rounded corners */
        cursor: pointer;           /* Hand cursor on hover */
        font-size: 18px;
        transition: background-color 0.3s; /* Transition effect */
        width: 30px;
        height: 30px;
    }

    #downloadAll:hover {
        background-color: #2980b9; /* Darker blue on hover */
    }

    /* ========== show pdf on the same page ========== */
    .pdfModal {
        display: none; 
        position: fixed;
        z-index: 99; 
        left: 0;
        top: 0;
        width: 100%; 
        height: 100%; 
        overflow: auto; 
        margin-top: 60px;
        background-color: rgba(0,0,0,0.7);
    }

    /* ---------- div contains the iframe and close icon ---------- */
    .pdfModal .pdfModal-content {
        background-color: white;
        margin: 1px auto;
        padding: 0 20px;
        border-radius: 10px;
        width: 80%;
        height: 98vh;
    }

    /* ---------- The iframe (the research) ---------- */
    .pdfModal .pdfModal-content iframe{
        border-radius: 10px;
        border: none;
        width: 100%;
        height: 93.5%;
    }
    /* ---------- The close icon of the iframe ---------- */
    .pdfModal .pdfModal-content .closebutton {
        color: #8cc4df;
        float: right;
        font-size: 26px;
        font-weight: bold;
    }

    .closebutton:hover,
    .closebutton:focus {
        color: #000;
        text-decoration: none;
        cursor: pointer;
    }

    /* -------- Responsive ---------- */

    /* ===================================== */
    /* ========== Portrait Screen ========== */
    /* ===================================== */
    @media screen and (orientation: portrait){
        .home-section-container{
            width: 100%;
            left: 0;
        }

        .container{
            display: block;
            padding: 0;
            width: 100%;
            scrollbar-width: none;
        }
        .headerContent h1{
            font-size: 18px;
        }
        .container::-webkit-scrollbar {
            width: 0px;
            background: transparent; 
        }

        main.table{
            width: 94%;
            margin-bottom: 0.8rem;
        }

        .description, .comment {
            width: 94%;
            margin: 0 auto;
            min-height: 150px;
            margin-bottom: 0.8rem;
        }

        .expanded{
            position: fixed;
            top: 70px;
            left: 10%;
        }

        .initial_decision_form{
            justify-content: space-between;
            margin-left: 3%;
            width: 94%;
        }

        .initial_decision_button{
            min-width: 170px;
            padding: 12px 20px;
        }
    }
    /*@media screen and (max-width: 350px) and (orientation: portrait){
        .outer{
            margin-top: 30px;
            width: 96%;
            margin-left: 2%;
        }
        img{
            width:200px;
            height: 270px;
        }
        .card-container .card{
            width: 210px;
            height: 280px;
            padding: 5px;
        }
        .card-container .card1{
            margin: -240px 0;
        }
        .description, .comment{
            margin-top: 10px;
            width: 96%;
            margin-left: 2%;
            height: 200px;
        }
        #decisionButton{
            width: 94%;
            margin-top: 10px;
            margin-left: 3%;
            margin-bottom: 30px;
        }

        .pdfModal .pdfModal-content{
            margin-top: 80px;
            width: 100%;
            height: 80vh;
            overflow: hidden;
        }
        iframe{
            height: 80vh;
        }
        .expanded{
            left: 0;
            height: 60vh;
        }
        .decisionModal .decisionModal-content{
            left: 0;
            width: 100%;
        }
    }
    @media screen and (min-width: 350px) and (max-width: 530px) and (orientation: portrait){
        .outer{
            margin-top: 30px;
            width: 96%;
            margin-left: 2%;
        }
        .description, .comment{
            margin-top: 10px;
            width: 96%;
            margin-left: 2%;
            height: 200px;
        }
        #decisionButton{
            width: 94%;
            margin-top: 10px;
            margin-left: 3%;
            margin-bottom: 30px;
        }

        .pdfModal .pdfModal-content{
            margin-top: 80px;
            width: 100%;
            height: 80vh;
            overflow: hidden;
        }
        iframe{
            height: 80vh;
        }
        .expanded{
            left: 0;
            height: 60vh;
        }
        .decisionModal .decisionModal-content{
            left: 0;
            width: 100%;
        }
    }

    @media screen and (min-width: 530px) and (max-width: 700px) and (orientation: portrait){
        .card-container{
            flex-direction: row;
            margin: 0;
        }
        .card-container .card0{
            margin: 0;
        }
        .card-container .card1{
            margin: 0 -160px;
        }
        .outer{
            margin-top: 30px;
            width: 96%;
            margin-left: 2%;
        }
        .description, .comment{
            margin-top: 10px;
            width: 96%;
            margin-left: 2%;
            height: 200px;
        }
        #decisionButton{
            width: 94%;
            margin-top: 10px;
            margin-left: 3%;
            margin-bottom: 30px;
        }

        .pdfModal .pdfModal-content{
            margin-top: 80px;
            width: 100%;
            height: 80vh;
            overflow: hidden;
        }
        iframe{
            height: 80vh;
        }
        .expanded{
            left: 0;
            height: 60vh;
        }
        .decisionModal .decisionModal-content{
            left: 0;
            width: 100%;
        }
    }

    {{{mediaQuery}}}

    @media screen and (min-width: 700px) and (max-width: 1200px) and (orientation: portrait){
        .container{
            display: grid;
            grid-template-columns: repeat(4, 160px);
            grid-template-rows: repeat(3, 220px);
            row-gap: 10px;
            column-gap: 10px;
        }
        .card-container .card0{
            margin: 0;
        }
        .card-container .card1{
            margin: 0 -100px;
        }
        .card-container{
            flex-direction: row;
            margin-top: 0;
        }
        .outer{
            margin-top: 30px;
            width: 96%;
            margin-left: 2%;
            grid-column: 1/5;
            grid-row: 1/3;
        }
        .description{
           grid-column: 1/3;
        }
        .comment{
            grid-column: 3/5;
        }
        #decisionButton{
            grid-column: 1/5;
        }
    }*/
    </style>
    <style>
    /* ====================================== */
    /* ========== Landscape Screen ========== */
    /* ====================================== */ 

    
    /*@media (max-width: 97px) and (orientation: landscape){
        .container{
            display: block;
            overflow-y: auto;
        }
        .card-container{
            flex-direction: row;
            margin-top: 0;
        }
        

        .headerContent h1{
            font-size: 18px;
        }
        .container::-webkit-scrollbar {
            width: 0px;
            background: transparent; 
        }

        main.table{
            width: 94%;
            margin-bottom: 0.8rem;
        }

        .description, .comment {
            width: 94%;
            margin: 0 auto;
            min-height: 150px;
            margin-bottom: 0.8rem;
        }

        .initial_decision_form{
            justify-content: space-between;
            margin-left: 3%;
            width: 94%;
        }

        .initial_decision_button{
            min-width: 170px;
            padding: 12px 20px;
        }
        .expanded{
            width: 80%;
        }
    }*/

    @media screen and (orientation: landscape) and (min-width: 1550px) {
        .expanded{
            width: 90%;
        }
        main.table {
            max-width: 1200px;
        }
        .description{
            max-width: 600px;
        }
        .comment{
            max-width: 600px;
        }
    }

    /*{{mediaQueryLandscape 570 -190}}
    @media screen and (min-width: 720px) and (max-width: 920px) and (orientation: landscape){
        .container{
            display: block;
            overflow-y: auto;
            height: 122vh;
        }
        .card-container{
            flex-direction: row;
            margin-top: 0;
        }
        .card-container .card0{
            margin: 0;
        }
        .card-container .card1{
            margin: 0 -120px;
        }
        .description, .comment{
            margin-top: 10px;
            height: 200px;
        }
        #decisionButton{
            margin-top: 10px;
            width: 96%;
            margin-left: 2%;
        }
        .sidebar.open ~ .home-section .card-container .card1{
            margin: 0 -200px;
        }
    }

    {{{mediaQueryLandscape}}}*/

    /* ---------- from 900px till 1180px will display 3 cards ---------- */
    /* ---------- till 1070px if the sidebar open the screen display 2 cards ---------- */
    /*@media screen and (min-width: 900px) and (orientation: landscape){
        .container{
            justify-content: center;
        }
    }*/
</style>

